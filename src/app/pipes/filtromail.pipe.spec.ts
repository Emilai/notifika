import { FiltromailPipe } from './filtromail.pipe';

describe('FiltromailPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltromailPipe();
    expect(pipe).toBeTruthy();
  });
});
